---
description: Tidy the vault: indexes, links between notes, state files, session log.
---

Run the `vault-groom` skill at **session scope**.

Work out what changed from git, or from file modification times if the vault is not in git. Do the
mechanical updates directly. Collect anything that is authoring rather than bookkeeping and propose
it at the end.

Finish with the coverage report. Every structural file in every folder you touched has to appear in
it, including the ones you skipped and why.

Never commit and never push.
