---
description: Set up or update your vault profile: root, structure, and gated paths.
---

Run the Onboarding section of the `vault-capture` skill in **update mode**.

If `~/.claude/vault-keeper/profile.md` already exists, read it first and ask only about what has
changed or what is missing. Do not restart the interview from scratch.

Pay particular attention to the gated paths. Those are what stop every one of these skills from
writing somewhere it should not, so it is worth confirming the list is still right rather than
assuming it is.

Show the updated file when you are done.
